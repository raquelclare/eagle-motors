{{> cars/nav-bar}}
<div class="container">
    <div class="row">
        <div class="col-md-4">
            <br>
            <h2>Available cars</h2>
        </div>
    </div>
    {{!--
    <div class="row">
        {{#each cars}} {{#unless this.sold}}
        <div class="col-md-4 car-col" id="{{this.id}}">
            <div class="car-stats">
                <img class="car-img" src="{{this.photo}}">
                <p>Make: {{this.make}}</p>
                <p>Model: {{this.model}} </p>
                <p>Year: {{this.year}} </p>
                <p>Exterior: {{this.color}} </p>
                <p>Miles: {{this.miles}} miles </p>
                <p>Price: ${{this.price}} </p> <br>
            </div>
        </div>
        {{/unless}} {{/each}}
        <!-- {{! An idea here would be to also add some kind of button to alert if someone has shown interest in the car }} -->
    </div>--}}
    <div class="row">
        {{!--
        <div class="card-group">--}} {{#each cars}} {{#unless this.sold}}

            <div class="card card-outline-danger" style="width: 20rem;" id="{{this.id}}">
                <img class="card-img-top" src="{{this.photo}}">
                <div class="card-block">
                    <h4 class="card-title">{{this.make}} {{this.model}}</h4>

                    <p class="card-text">Year: {{this.year}} </p>
                    <p class="card-text">Exterior: {{this.color}} </p>
                    <p class="card-text">Miles: {{this.miles}} miles </p>
                    <p class="card-text">Price: ${{this.price}} </p>
                    <a href="#" class="btn btn-primary">See more</a>
                </div>
            </div>


            {{/unless}} {{/each}} {{!--
        </div>--}}
        <!-- {{! An idea here would be to also add some kind of button to alert if someone has shown interest in the car }} -->
    </div>
    <br>
    {{> cars/footer}}
</div>
<!-- Custom Script -->
<script src="js/view-cars.js" type="text/javascript"></script>
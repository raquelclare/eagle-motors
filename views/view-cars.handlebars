<nav class="navbar navbar-toggleable-md navbar-inverse fixed-top bg-inverse">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="/"><img src="./assets/images/eagle-motors-logo.png" id="logo"></a>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
    </ul>
        <a class="btn btn-outline-success" href="/login" role="button">ADMIN</a>
    </div>
</nav>
<div class="container">
    <div class="row">
        <div class="col-md-4">
            <br>
            <h2>Car Inventory</h2>
        </div>
    </div>
    <div class="row">
        {{#each cars}} {{#unless this.sold}}
        <div class="col-md-4 car-col" id="{{this.id}}">
              <div class="panel-body car-stats">
                <img class="car-img" src="{{this.photo}}">
                <p>Make: {{this.make}}</p>
                <p>Model: {{this.model}} </p>
                <p>Year: {{this.year}} </p>
                <p>Exterior: {{this.color}} </p>
                <p>Miles: {{this.miles}} miles </p>
                <p>Price: ${{this.price}} </p> <br>
              </div>
              {{!--Just testing panels below for editing purposes--}}
              <div class="panel panel-default">
                <div class="panel-body">
                    Panel content
                </div>
              <div class="panel-footer">Panel footer</div>
             </div>
        </div>
        {{/unless}} {{/each}}
        {{!-- An idea here would be to also add some kind of button to alert if someone has shown interest in the car --}}
    </div>

      <hr>

      <footer>
        <p>&copy; B&R Eagle Motors 2017</p>
      </footer>
</div>
<!-- Custom Script -->
<script src="js/view-cars.js" type="text/javascript"></script>

